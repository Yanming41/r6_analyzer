# ── Core ML ──────────────────────────────────────────────────────────────────
# Install PyTorch FIRST via setup_env.bat (needs CUDA wheel, not this file)
# torch>=2.1.0
# torchvision>=0.16.0

# ── Transformers stack ────────────────────────────────────────────────────────
transformers>=4.45.0
accelerate>=0.30.0
huggingface_hub>=0.24.0

# 4-bit quantization (optional; Windows wheels may differ)
# Install manually if needed: pip install bitsandbytes --prefer-binary
bitsandbytes>=0.43.0

# ── Video / image processing ──────────────────────────────────────────────────
# decord provides fast GPU-accelerated video reading
decord>=0.6.0
opencv-python>=4.8.0
Pillow>=10.0.0
numpy>=1.24.0

# ── Utilities ─────────────────────────────────────────────────────────────────
tqdm>=4.65.0
ffmpeg-python>=0.2.0
